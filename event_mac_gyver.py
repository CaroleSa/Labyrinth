import os
from labyrinth_position import *









    """finale_position_dict = ("{}: {}".format(cle+4, valeur+20))
    """
    print(mouv_dict)


mouv_dict={0: 520}
while :
    if event.key == K_RIGHT:
        mac_gyver_location = mac_gyver_location.move(40, 0)
        for cle, valeur in mouv_dict.items():
            mouv_dict[cle+40]=valeur
            finale_position= ("({}, {})".format(cle+4, valeur+20))
            init_event(finale_position) # rafraichissement et recollage des images


